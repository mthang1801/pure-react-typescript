import { IRootState } from "./root.interfaces";

export interface ICategoryState {
	stateListCategory: IRootState;
	stateUpdateIndexCategory: IRootState;
	stateCategoryById: IRootState;
	stateCreateCategory: IRootState;
	stateUpdateCategory: IRootState;
}
